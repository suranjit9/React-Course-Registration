

const SingalData = ({data}) => {
    
    const {title}=data;
    return (
        <div>
            <h1 className="text-xl "> {title}</h1>
        </div>
        

    );
};

export default SingalData;